import { createTheme } from "@material-ui/core";

const theme = createTheme({
    primary :{
        palette:{
            light: "#63a4fff",
            main: "#1976d2",
            dark: "#004ba0",
            contrastText: "#ecfad8"
        }
    }
})
export default theme;